
#ifndef MAX_FLOW_MPM_H
#include <vector>
int mpm(const std::vector<std::vector<size_t>>& adj, std::vector<std::vector<int>> capacity, size_t source, size_t sink);
#define MAX_FLOW_MPM_H

#endif //MAX_FLOW_MPM_H
